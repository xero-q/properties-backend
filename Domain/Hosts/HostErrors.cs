using SharedKernel;

namespace Domain.Hosts;

public static class HostErrors
{
    // public static Error NotFound(int hostId) => Error.NotFound(
    //     "Hosts.NotFound",
    //     $"The host with the Id = '{hostId}' was not found");
}
